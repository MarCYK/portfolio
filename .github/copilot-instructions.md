

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.0 -->
<!-- Updated: 2026-05-10T02:30:11.928Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
src/components/MobileMenu.tsx ← icons
src/components/PageShell.tsx ← MobileMenu, SiteFooter, SiteHeader
src/components/ProjectCard.tsx ← icons
src/components/SiteHeader.tsx ← icons, header/ThemeToggle, header/SoundToggle, header/CanvasToolbar
src/lib/audio.ts ← song-data
src/lib/canvas-engine.ts ← song-data, audio, canvas-events
src/components/SiteFooter.tsx ← icons
```

## changes (last 10 commits — 5 days ago)
```
src/app/about/page.tsx                        ~AboutPage
src/app/projects/[slug]/page.tsx              +generateStaticParams  +generateMetadata  +ProjectPage
src/app/projects/page.tsx                     ~ProjectsPage
src/app/words/[slug]/page.tsx                 ~getPostBySlug  ~generateMetadata
src/app/words/page.tsx                        ~WordsRow  ~WordsPage
src/components/CanvasHome.tsx                 +stopAirCamera  ~CanvasHome  ~handleDraw  ~animate
src/components/MobileMenu.tsx                 ~MobileMenu
src/components/PageShell.tsx                  +PageShell
src/components/ProjectCard.tsx                ~ProjectCard
src/components/SiteHeader.tsx                 ~SiteHeader
src/components/header/CanvasToolbar.tsx       +CanvasToolbar
src/components/header/SoundToggle.tsx         +readSoundPreference  +useSound  +SoundToggle
src/components/header/ThemeToggle.tsx         +useTheme  +ThemeToggle
src/components/icons.tsx                      ~LogoDiamond
src/contexts/CanvasContext.tsx                +CanvasProvider  +useCanvas
src/data/posts.ts                             +getPostBySlug
src/lib/audio.ts                              +readSoundPreference  +playNote  ~playNote  ~initAudio
src/lib/canvas-engine.ts                      +hexToRgb  +lerp  +sampleGradient  +mixRgb
src/lib/projects.ts                           +isInternalProject  +getInternalProjectBySlug  +getInternalProjectSlugs
src/lib/song-data.ts                          +computeRows  +rowToNote  +midiPitchToRow  ~rowToNote
```

## src

### src/STRUCTURE.md
```
h1 Source Structure
h2 Boundary Rules
h2 Current Interaction Model
h2 Notes
code-fence text
code-fence plain
```

### src/app/about/page.tsx
```
component AboutPage
```

### src/app/globals.css
```
var --background
var --bg
var --surface1
var --surface2
var --surface3
var --border
var --border-visible
var --text1
var --text2
var --text3
var --text4
var --accent
var --accent-subtle
var --success
var --success-bg
var --warning
var --warning-bg
var --error
var --error-bg
var --font-display
var --font-body
var --font-mono
var --text-display
var --text-heading
var --text-subheading
```

### src/app/layout.tsx
```
component RootLayout
```

### src/app/projects/[slug]/page.tsx
```
component ProjectPage
```

### src/app/projects/page.tsx
```
component ProjectsPage
```

### src/app/words/[slug]/page.tsx
```
component PostPage
```

### src/app/words/page.tsx
```
component WordsRow
component WordsPage
```

### src/components/CanvasHome.tsx
```
component CanvasHome
hook useCanvas
hook useRef
hook useEffect
handler onMouseDown
handler onMouseMove
handler onTouchStart
handler onTouchMove
handler onWindowNotePlayed
```

### src/components/MobileMenu.tsx
```
component MobileMenu
hook useCanvas
hook useState
hook useEffect
handler onClick
```

### src/components/PageShell.tsx
```
component PageShell
props PageShellProps
```

### src/components/ProjectCard.tsx
```
component ProjectCard
handler onNode
```

### src/components/SiteHeader.tsx
```
component SiteHeader
hook usePathname
hook useTheme
hook useSound
hook useCanvas
hook useState
hook useEffect
handler onToggle
handler onClick
```

### src/components/header/CanvasToolbar.tsx
```
component CanvasToolbar
hook useCanvas
hook useState
hook useRef
hook useEffect
handler onKeyDown
handler onClick
```

### src/components/header/SoundToggle.tsx
```
component SoundToggle
hook useSound
hook useCanvas
hook useState
hook useEffect
handler onClick
```

### src/components/header/ThemeToggle.tsx
```
component ThemeToggle
props ThemeToggleProps
hook useTheme
hook useCanvas
hook useState
hook useEffect
handler onClick
```

### src/components/icons.tsx
```
component LogoDiamond
export LogoDiamond
export PROJECT_ICONS
```

### src/contexts/CanvasContext.tsx
```
component CanvasProvider
hook useRef
hook useMemo
hook useCanvas
hook useContext
export CanvasProvider
```

### src/data/posts.ts
```
export function getPostBySlug(slug) → Post | null
```

### src/lib/audio.ts
```
export interface AudioState
  audioCtx: AudioContext | null
  player: Player | null
  soundEnabled: boolean
export function createAudioState() → AudioState
export async function initAudio(state) → Promise<void>
export function playNote(state, rowIndex, velocity = 0.8, totalRows = 30) → void
```

### src/lib/canvas-engine.ts
```
export interface CanvasState
  rows: number
  energy: Float32Array
  rowGlow: Float32Array
  timeOffset: number
  strokeColor: string
  bgColor: string
  theme: CanvasTheme
  customStrokeColor: string | null
export type CanvasTheme
export function getDefaultCanvasColors(theme)
export function createCanvasState(theme, rows) → CanvasState
export function updateRows(state, rows) → void
export function tickMusic(state, audio) → void
export function drawFrame(canvas, ctx, state, audio,) → void
export function getRowAtY(canvasHeight, clientY, rows) → number
```

### src/lib/projects.ts
```
export function getInternalProjectBySlug(slug) → InternalProject | null
export function getInternalProjectSlugs() → string[]
```

### src/lib/song-data.ts
```
export function computeRows(drawableHeight) → number
export function rowToNote(rowIndex, totalRows) → string
export function midiPitchToRow(midi, totalRows) → number
export function midiToName(midi) → string
```

### src/types/index.ts
```
export interface Project
  title: string
  description: string
  date: string
  href: string
  external?: boolean
  icon: string
  status?: string
  content?: string[]
export interface InternalProject
  external: false
  content: string[]
export interface Post
  date: string
  title: string
  href: string
  author?: string
  content?: string[]
```

### src/app/page.tsx
```
component HomePage
```

### src/components/SiteFooter.tsx
```
component SiteFooter
```
