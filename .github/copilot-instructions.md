

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v7.0.1 -->
<!-- Updated: 2026-06-16T19:17:22.159Z -->
<!-- sigmap: version=7.0.1 -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures
## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| To rank files by topic | `sigmap --query "<topic>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

Always run `sigmap ask` (or `sigmap --query`) before searching for files relevant to a task.

## deps
```
src/components/MobileMenu.tsx ← icons
src/components/PageShell.tsx ← MobileMenu, SiteFooter, SiteHeader
src/components/ProjectCard.tsx ← icons
src/components/SiteHeader.tsx ← icons, header/ThemeToggle, header/SoundToggle, header/CanvasToolbar
src/lib/audio.ts ← song-data
src/lib/canvas-engine.ts ← song-data, audio, canvas-events
src/components/SiteFooter.tsx ← icons
```

## changes (last 10 commits — 5 weeks ago)
```
src/app/about/page.tsx                        ~AboutPage
src/app/projects/[slug]/page.tsx              ~generateStaticParams
src/app/projects/page.tsx                     ~ProjectsPage
src/app/words/[slug]/page.tsx                 ~getPostBySlug  ~generateMetadata
src/app/words/page.tsx                        ~WordsRow  ~WordsPage
src/components/CanvasHome.tsx                 +stopAirCamera  ~CanvasHome  ~handleDraw  ~animate
src/components/MobileMenu.tsx                 ~MobileMenu
src/components/PageShell.tsx                  +PageShell
src/components/ProjectCard.tsx                ~ProjectCard
src/components/SiteHeader.tsx                 ~SiteHeader
src/components/header/CanvasToolbar.tsx       +CanvasToolbar
src/components/header/SoundToggle.tsx         +readSoundPreference  +useSound  +SoundToggle
src/components/header/ThemeToggle.tsx         +useTheme  +ThemeToggle
src/components/icons.tsx                      ~LogoDiamond
src/contexts/CanvasContext.tsx                +CanvasProvider  +useCanvas
src/data/posts.ts                             +getPostBySlug
src/lib/audio.ts                              +readSoundPreference  +playNote  ~playNote  ~initAudio
src/lib/canvas-engine.ts                      +hexToRgb  +lerp  +sampleGradient  +mixRgb
src/lib/projects.ts                           +isInternalProject  +getInternalProjectBySlug  +getInternalProjectSlugs
src/lib/song-data.ts                          +computeRows  +rowToNote  +midiPitchToRow  ~rowToNote
```

## src

### src/STRUCTURE.md
```
h1 Source Structure
h2 Boundary Rules
h2 Current Interaction Model
h2 Notes
code-fence text
code-fence plain
```

### src/app/about/page.tsx
```
component AboutPage
```

### src/app/globals.css
```
var --background
var --bg
var --surface1
var --surface2
var --surface3
var --border
var --border-visible
var --text1
var --text2
var --text3
var --text4
var --accent
var --accent-subtle
var --success
var --success-bg
var --warning
var --warning-bg
var --error
var --error-bg
var --font-display
var --font-body
var --font-mono
var --text-display
var --text-heading
var --text-subheading
```

### src/app/layout.tsx
```
component RootLayout
```

### src/app/projects/[slug]/page.tsx
```
component ProjectPage
```

### src/app/projects/page.tsx
```
component ProjectsPage
```

### src/app/words/[slug]/page.tsx
```
component PostPage
```

### src/app/words/page.tsx
```
component WordsRow
component WordsPage
```

### src/components/CanvasHome.tsx
```
component CanvasHome
hook useCanvas
hook useRef
hook useEffect
handler onMouseDown
handler onMouseMove
handler onTouchStart
handler onTouchMove
handler onWindowNotePlayed
```

### src/components/MobileMenu.tsx
```
component MobileMenu
hook useCanvas
hook useState
hook useEffect
handler onClick
```

### src/components/PageShell.tsx
```
component PageShell
props PageShellProps
```

### src/components/ProjectCard.tsx
```
component ProjectCard
handler onNode
```

### src/components/SiteHeader.tsx
```
component SiteHeader
hook usePathname
hook useTheme
hook useSound
hook useCanvas
hook useState
hook useEffect
handler onToggle
handler onClick
```

### src/components/header/CanvasToolbar.tsx
```
component CanvasToolbar
hook useCanvas
hook useState
hook useRef
hook useEffect
handler onClick
```

### src/components/header/SoundToggle.tsx
```
component SoundToggle
hook useSound
hook useCanvas
hook useState
hook useEffect
handler onClick
```

### src/components/header/ThemeToggle.tsx
```
component ThemeToggle
props ThemeToggleProps
hook useTheme
hook useCanvas
hook useState
hook useEffect
handler onClick
```

### src/components/icons.tsx
```
component LogoDiamond
export LogoDiamond
export PROJECT_ICONS
```

### src/contexts/CanvasContext.tsx
```
component CanvasProvider
hook useRef
hook useState
hook useCallback
hook useMemo
hook useCanvas
hook useContext
export CanvasProvider
```

### src/data/posts.ts
```
export function getPostBySlug(slug) → Post | null  :47-49
```

### src/lib/audio.ts
```
export interface AudioState  :7-11
  audioCtx: AudioContext | null  :8-8
  player: Player | null  :9-9
  soundEnabled: boolean  :10-10
export function createAudioState() → AudioState  :23-29
export async function initAudio(state) → Promise<void>  :31-42
export function playNote(state, rowIndex, velocity = 0.8, totalRows = 30) → void  :44-54
```

### src/lib/canvas-engine.ts
```
export interface CanvasState  :98-117
  rows: number  :99-99
  energy: Float32Array  :100-100
  rowGlow: Float32Array  :101-101
  rowPaintMask: Uint8Array  :102-102
  rowPaintR: Uint8Array  :103-103
  rowPaintG: Uint8Array  :104-104
  rowPaintB: Uint8Array  :105-105
  timeOffset: number  :106-106
export type CanvasTheme  :82-82
export function getDefaultCanvasColors(theme)  :84-84
export function createCanvasState(theme, rows) → CanvasState  :119-142
export function updateRows(state, rows) → void  :144-161
export function tickMusic(state, audio) → void  :163-187
export function drawFrame(canvas, ctx, state, audio,) → void  :189-290
export function getRowAtY(canvasHeight, clientY, rows) → number  :383-389
```

### src/lib/projects.ts
```
export function getInternalProjectBySlug(slug) → InternalProject | null  :10-16
export function getInternalProjectSlugs() → string[]  :18-20
```

### src/lib/song-data.ts
```
export function computeRows(drawableHeight) → number  :31-34
export function rowToNote(rowIndex, totalRows) → string  :36-39
export function midiPitchToRow(midi, totalRows) → number  :41-44
export function midiToName(midi) → string  :46-50
```

### src/types/index.ts
```
export interface Project  :1-10
  title: string  :2-2
  description: string  :3-3
  date: string  :4-4
  href: string  :5-5
  external?: boolean  :6-6
  icon: string  :7-7
  status?: string  :8-8
  content?: string[]  :9-9
export interface InternalProject  :12-15
  external: false  :13-13
  content: string[]  :14-14
export interface Post  :17-23
  date: string  :18-18
  title: string  :19-19
  href: string  :20-20
  author?: string  :21-21
  content?: string[]  :22-22
```

### src/app/page.tsx
```
component HomePage
```

### src/components/SiteFooter.tsx
```
component SiteFooter
```
